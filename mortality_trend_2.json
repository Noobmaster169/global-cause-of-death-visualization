{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "title": "Underdeveloped Country Mortality Rates",
  "width": 300,
  "height": 100,
  "data": {
    "url": "data/total_deaths_by_country_with_population.csv"
  },
  "transform": [
    {"filter": "datum.country == 'Ethiopia' || datum.country == 'South Africa' || datum.country == 'Laos' || datum.country == 'Nigeria'"},
    {"calculate": "datum.total_deaths / datum.total_population", "as": "Mortality Rate"}
  ],
  "mark": {
    "type": "line",
    "strokeWidth": 3
  },
  "encoding": {
    "x": {
      "field": "year",
      "type": "ordinal",
      "title": "Year"
    },
    "y": {
      "field": "Mortality Rate",
      "type": "quantitative",
      "title": "Mortality Rate",
      "scale": {
        "domain": [0.005, 0.02]
      }
    },
    "color": {
      "field": "country",
      "type": "nominal",
      "title": "Country",
      "scale": {
        "scheme": "yellowgreen"
      }
    },
    "tooltip": [
      {"field": "country", "type": "nominal", "title": "Country"},
      {"field": "year", "type": "quantitative", "title": "Year"},
      {"field": "Mortality Rate", "type": "quantitative", "format": ".4%", "title": "Mortality Rate"}
    ]
  }
}